<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Profile View</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="rounded-md bg-red-500 px-3 py-2 text-sm mb-1 inline-block" href="/logout">LOG OUT</a>
        </div>
        <div class="flex item-starts gap-3 mt-3">
            <div class="w-10 h-10 rounded-md overflow-hidden">
                <a href="/profile/upload"><img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilePic%>" alt=""></a>
            </div>
            <h3 class="text-3xl mb-5">Hello,<%= user.username %> </h3>
        </div>
        <h5 class="mb-5">You can create a task</h5>
        <form action="/post" method="post">
            <textarea placeholder="Create your task here" class="block resize-none bg-transparent border-2 border-zinc-200 rounded-md w-1/3" name="content"></textarea>
            <input type="submit" class="bg-blue-500 rounded-md px-3 py-2 mt-2" value="Create Task">
        </form>
        <div class="mt-10 posts"> 
            <h3 class="text-2xl">Your tasks</h3>
            <div class="postcontainer mt-5">
                    <% user.post.forEach((e) => { %>
                <div class="post w-1/3 p-5 border-[2px] mb-3 rounded-md border-zinc-800">
                        <p class="text-sm tracking-tight"><%= e.content %></p>
                        <div class="btns flex gap-4 mt-5">
                        <a class="text-blue-500" href="/edit/<%=e._id%>">Edit</a>
                        <a class="text-red-500" href="/delete/<%=e._id%>">Delete</a>
                        </div>
                    </div>

                    <%})%>               
            </div>
        </div>
    </div>
</body>
</html>