<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Profile View</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="rounded-md bg-red-500 px-3 py-2 text-sm mb-1 inline-block" href="/logout">LOG OUT</a>
        </div>
        <h5 class="mb-5">Edit your task</h5>
        <form action="/update/<%=post._id%>" method="post">
            <textarea class="block resize-none bg-transparent border-2 border-zinc-200 rounded-md w-1/3" name="content"><%= post.content%></textarea>
            <input type="submit" class="bg-blue-500 rounded-md px-3 py-2 mt-2" value="Edit Task">
        </form>
    </div>
</body>
</html>